#!/usr/bin/env bash
# {{ ansible_managed }}

##
## USAGE: This is not to be run manually, but is only used as a source
## of credentials by backup-to-s3.sh.
##

# Unofficial Bash strict mode
set -eEfuo pipefail
IFS=$'\n\t'

DB_USER="root"
DB_PASS="{{ lookup('file', 'credentials/mysql/root') }}"
