# {{ ansible_managed }}

[default]
aws_access_key_id = {{ lookup('file', 'credentials/aws/backup_access_key') }}
aws_secret_access_key = {{ lookup('file', 'credentials/aws/backup_secret_key') }}

output = text
region = eu-west-1
