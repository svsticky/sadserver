---
name: "Lint"
on:
  "pull_request"
jobs:
  build:
    runs-on: "ubuntu-latest"

    steps:
      - uses: "actions/checkout@v2"

      - name: "install Nix"
        uses: "cachix/install-nix-action@v12"

      - name: "Lint our Python files"
        run: "(cd ansible; nix run -c black deploy.py)"

      - name: "Typecheck our Python files"
        run: "(cd ansible; nix run -c mypy deploy.py --strict)"

      - name: "Lint our ansible files"
        run: "(cd ansible; nix run -c ansible-lint --exclude=tasks --exclude=playbooks)"
