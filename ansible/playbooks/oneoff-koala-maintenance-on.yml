---
- hosts: all

  vars_files:
      - "../vars.yml"

  tasks:
    - name: "enable Koala's maintenance mode"
      file:
        state: "touch"
        dest: "/var/www/koala.{{ canonical_hostname }}/MAINTENANCE_MODE"
      become: true
      become_user: "koala"
