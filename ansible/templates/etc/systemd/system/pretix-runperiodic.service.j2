# {{ ansible_managed }}

[Unit]
Description=Clear expired pretix sessions
Requires=mysql.service

[Service]
User=pretix
Group=pretix
Type=oneshot
Environment="VIRTUAL_ENV=/var/pretix/venv"
Environment="PATH=/var/pretix/venv/bin:/usr/local/bin:/usr/bin:/bin"
ExecStart=/var/pretix/venv/bin/python -m pretix runperiodic
WorkingDirectory=/var/pretix

[Install]
WantedBy=multi-user.target
