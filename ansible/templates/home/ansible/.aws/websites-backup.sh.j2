#!/bin/bash

FILE=websites-$(date +'%Y-%m-%d').tar.gz

tar cvf - /var/www | gzip -9 > /tmp/$FILE && aws s3 cp /tmp/$FILE s3://sticky-automatic-backups/websites/ && rm /tmp/$FILE

# {{ ansible_managed }}
