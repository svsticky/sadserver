skip_list:
  # We don't want to always use explicit versions for git checkouts
  - 401
  # Comparing to an empty string is sometimes just necessary, ansible-lint is just too strict here
  # Complete discussion here: https://github.com/ansible-community/ansible-lint/issues/450
  - 602
