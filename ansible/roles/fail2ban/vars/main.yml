---
fail2ban_config:
  loglevel: "WARNING"
  ignoreip: "127.0.0.1/8 131.211.0.0/16"
  bantime: 600
  maxretry: 6
  banaction: "iptables-multiport"

  services:
    - name: "sshd"
      port: "ssh"
      filter: "sshd"
      logpath: "/var/log/auth.log"
