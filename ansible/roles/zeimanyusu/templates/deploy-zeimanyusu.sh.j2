#!/usr/bin/env bash
# {{ ansible_managed }}

set -efuo pipefail

cd "{{ zeimanyusu_home_dir }}"

echo "Pulling latest image..."
docker compose pull

echo "Starting container..."
docker compose up -d

docker image prune -f

echo "Deployment finished! Check logs with: docker logs -f zeimanyusu"
