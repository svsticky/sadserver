---
- hosts: all
  user: "ansible"
  force_handlers: true

  vars_files:
      - ../vars.yml

  tasks:
    - name: "ensure koala is running"
      service:
        name: "koala"
        enabled: true
        state: "started"
      become: true
